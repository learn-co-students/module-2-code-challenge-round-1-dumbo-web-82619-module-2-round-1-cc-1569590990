<h1>New Appearance</h1>

<%= form_for @appearance do |f| %>

<%= f.label :guest_id %>
<%= f.collection_select :guest_id, @guests, :id, :name%>

<%= f.label :episode_id %>
<%= f.collection_select :episode_id, @episodes, :id, :number%>

<%= f.label :guest_rating %>
<%= f.number_field :guest_rating, in: 1...6%>

<%= f.submit%>

<%end %>

