<h1>Make an appearance!</h1>

<% if flash[:errors] %>
<ul>
  <% flash[:errors].each do |error| %>
  <li><%= error %><li>
  <% end %>
</ul>
<% end %>

<%= form_for @appearance do |f| %>
<%= f.label :guest_id %>
<%= f.collection_select :guest_id, @guests, :id, :name %><br>

<%= f.label :episode_id %>
<%= f.collection_select :episode_id, @episodes, :id, :date %><br>

<%= f.label :rating %>
<%= f.number_field :rating %><br>
<br>
<%= f.submit %>
<% end %>